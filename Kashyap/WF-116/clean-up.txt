Execute this cleanup as a standalone task in this chat. The instructions below are complete: do not ask for or require a prior chat, manifest, run ID, setup output, model response, companion prompt, or mode/command flag. Remove all matching workflow output regardless of when the prior run occurred. Use only Marketing Data Warehouse - Solstice Cloud, Weekly Attribution Reports - Solstice Cloud, Jira SCM, and Marketing Attribution Summary.

1. Drive: delete all workflow report Docs/files from Weekly Attribution Reports - Solstice Cloud; retain the folder.
2. Jira: delete every SCM issue and attachment. Retain the project itself; standalone setup recreates the one named baseline issue.
3. Teams: delete all posts/replies in Marketing Attribution Summary.
4. Sheet: restore Run Log to headers only and remove workflow-created tabs/objects. Verify each frozen source tab remains unchanged. If a source tab was changed, stop and report the unresolved source change rather than guessing a source baseline.

If any reset remains unresolved, stop and report the affected resource plainly. When finished, summarize the cleanup in plain language and confirm an empty output surface, empty Jira project, zero Teams messages, and frozen source tabs left untouched.
