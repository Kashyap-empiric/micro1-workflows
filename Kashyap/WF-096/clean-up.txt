Execute this cleanup as a standalone task in this chat. The instructions below are complete: do not ask for or require a prior chat, manifest, run ID, setup output, model response, companion prompt, or mode/command flag. Remove all matching workflow output regardless of when the prior run occurred. Use only Jira project TCW, RelayForge Research Documentation, testing client workflows → Engineering, and kashyapempiricinfotech-sys/relayforge-operations-platform.

1. Drive: retain every source attachment/document. Delete every Google Doc whose title begins [TCW] - 3rd Party Research -; standalone setup recreates the blank stale output Doc.
2. Jira: delete every issue, comment, and attachment in TCW. Retain the project itself; standalone setup recreates the deterministic queue.
3. Teams: delete every post and reply in testing client workflows → Engineering.
4. GitHub: verify the default-branch source fixture is unchanged; do not modify it.

If any reset remains unresolved, stop and report the affected resource plainly. When finished, summarize the cleanup in plain language and confirm empty TCW, retained Drive source inputs, zero Teams messages, and an unchanged source snapshot.
