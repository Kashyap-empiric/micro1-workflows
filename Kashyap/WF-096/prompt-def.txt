Run through the full third party research pipeline for the Jira ticket generated from the setup prompt. Output should be four things - a Google Doc, a Jira comment, a Microsoft Teams message (or a logged skip under the duplicate rule), and a final run report.

Before doing anything else, confirm read/write access to the "[TCW]", the GitHub repository "[https://github.com/kashyapempiricinfotech-sys/relayforge-operations-platform]", the existing Google Drive folder "[RelayForge Research Documentation]", Google Docs within that folder, and the Teams channel "[testing client workflows + Engineering]". If any resource is unavailable, or the Google Drive folder does not exist, stop and report exactly what failed. If more than one Google Drive folder matches that exact name, treat it the same way: stop and report every matching folder's name, path, and owner instead of guessing which one to use. Do not create replacement folders or use other tools. Once GitHub access is confirmed, record the repository's default branch name and its current HEAD commit SHA. That pair is your snapshot: use it for every repository read this run, even if new commits land on the branch while you're working.

Select the highest-priority issue in "[TCW]" where Status ≠ Done and the label is Research, Technical Research, Integration Research, or Architecture Research. Priority means Jira's native Priority field, ranked Highest > High > Medium > Low > Lowest. This is equivalent to the JQL: project = TCW AND status != Done AND labels in (Research, "Technical Research", "Integration Research", "Architecture Research") ORDER BY priority DESC, created ASC, key ASC. Break ties by earliest created date, then by the lowest issue key. Title, Description, Acceptance Criteria, Attachments, Comments, and Labels are extracted. Transition the ticket to state 'In Progress' and add a comment "Research has been started automatically on <YYYY-MM-DD HH:MM IST>". If there is no Jira ticket then return "No Jira ticket found in project [TCW]."

Only use the following project context sources, read from the repository snapshot recorded above, not from whatever is on the default branch at read time: repository README.md file; all content under docs/architecture/ directory; Jira project description; CONTRIBUTING.md file (if present); build and dependency configuration files (package.json, package-lock.json, yarn.lock, pnpm-lock.yaml, pom.xml, build.gradle, requirements.txt, go.mod, Cargo.toml); Dockerfile; CI/CD pipeline configuration (.github/workflows, Azure DevOps pipelines, GitLab CI if present); infrastructure-as-code configuration files (Terraform, Helm, Docker Compose, Kubernetes if present). Extract the following pieces of information: project overview; frontend framework and its version; backend framework and its version; database; storage; runtime versions; authentication method; deployment method; cloud platform; third party services integration (if any); package manager; build tool; coding style. Use repository configuration when repository configuration contradicts documentation. Use "Not specified in source" for any value that cannot be found.

Gather requirements only from the ticket description and acceptance criteria. Group them as follows: functional, non-functional, security, and compliance requirements. If any group is missing, write "Not specified in ticket."
Examine all the attachments, links to documents, API spec sheets, architecture diagrams, and design sheets linked to the chosen Jira issue. Only open linked documents that live inside the confirmed "[RelayForge Research Documentation]" Drive folder or are attached directly to the issue itself; treat any link pointing to a different Drive folder, a different account, or an external system as out of scope and report it as inaccessible rather than requesting broader access. Identify implementation restrictions, functional requirements, dependencies from UI, security requirements, compliance requirements, performance requirements, and integration assumptions. Identify the source document of each requirement identified. If any document is inaccessible, mention it as well.

List exactly eight candidate platforms that match the feature category of the ticket description. Ground the shortlist in three independent, verifiable adoption signals found via official sources as of the run date: (1) GitHub stars on the platform's official SDK or client repository, (2) weekly download count on the relevant package registry (npm, PyPI, or equivalent), and (3) inclusion in at least one third-party category comparison (e.g., G2, Capterra, StackShare). For each signal, save the exact source URL you used, plus an archived or dated snapshot of that page if your tools can grab one (e.g. a web.archive.org save), and keep both next to the value. Use only these three sources per candidate; don't run extra searches beyond what's needed to fill in the three signals. Record these three signal values for every candidate considered in the final run report, and select the eight platforms with the strongest combined signals; if you need judgment to break a near-tie, note your reasoning in the run report. If fewer than eight qualifying platforms exist, list all that qualify and note the shortfall in the final run report. Treat the run date as the as-of date for all vendor research: pricing, SDK release dates, and feature availability all reflect what those official sources show as of the run date, and if a source is dated after the run date, don't use it. Rank each platform according to feature set (40%), technology compatibility (30%), documentation (15%), and price (15%). Choose the top five for further evaluation. If there is a tie at the fifth position, pick the cheaper one in terms of estimated monthly cost for 10,000 MAUs.

Select and review exactly five platforms. For each platform, provide a brief description (2-3 sentences); features supported against extracted requirements; whether it is compatible with every technology identified in the detected project stack (Yes, Partial, or No), including frameworks, databases, runtimes, cloud platforms, deployment targets, and authentication mechanisms where applicable. SDK availability, maturity level, supported programming languages, official SDK repository, official documentation, example repositories (if available), migration guides (if available), and the date of the latest stable SDK release where publicly documented; integration complexity (Easy, Medium, or Complex) with a single sentence reasoning; documentation quality with a link to the official documentation and rating between 1 and 10; pricing information with specifics about the free tier, pricing model, cost per usage if applicable, and monthly price estimate for 10,000 monthly active users. Evaluate vendor maturity by documenting years in operation (if publicly available), enterprise support availability, published SLA (if available), API versioning policy, deprecation policy, SDK maintenance activity, and release cadence using only official vendor sources. Also include up to four risks, four advantages, four disadvantages, officially documented authentication methods (OAuth, API key, and/or JWT) and compliance certifications, implementation steps (maximum 10) covering account creation, credential generation, SDK installation, environment configuration, backend integration, frontend integration (if applicable), webhook configuration (if applicable), testing, monitoring setup, and production deployment, and one public authentication code example if officially available. Otherwise write "No public sample available." Cap research scope at up to eight official sources per platform (SDK repository, official documentation, pricing page, and up to five further official pages), saving the same source URL and archived snapshot you used for the candidate signals above. If you can't find a data point within that cap, write "Not publicly documented" instead of searching further.

Produce a comparison table with these columns, in this order: Platform, Feature Coverage (1-10), Tech Stack Compatibility (1-10), Integration Complexity, Estimated Monthly Cost (USD), SDK Quality (1-10), Documentation Quality (1-10), Security (1-10), Scalability (1-10), Community Support (1-10), and Overall Score. Score each 1-10 column against these bands, using your judgment to place each platform and citing the evidence for that placement in the run report. For Feature Coverage, 9-10 means it fully supports nearly all requirements extracted earlier in this prompt, 6-8 means it supports most with minor gaps, 3-5 means about half are supported, and 1-2 means few are. For Tech Stack Compatibility, score 10 if every stack item is Yes, 6-8 if Partial on one item, 3-5 if Partial on multiple items, and 1-2 if any item is No. For SDK Quality, 9-10 means the SDK is under active, well-supported development with broad official language coverage, 6-8 means it's maintained but with narrower language coverage or a slower release cadence, 3-5 means it's maintained but shows real signs of neglect (sparse commits, unaddressed issues) or very limited language support, and 1-2 means it's unmaintained, deprecated, or community-only; cite the specific evidence you used (last release date, issue response times, number of officially supported languages) to land on a band. For Documentation Quality, 9-10 means a complete quickstart, full API reference, runnable code samples, and migration guides are all present, 6-8 means it has a quickstart and API reference but is missing samples or migration guides, 3-5 means the docs are partial or outdated, and 1-2 means they're minimal or fragmented. For Security, 9-10 means both SOC 2 and GDPR (or equivalent) are publicly documented, 6-8 means one of the two is documented, 3-5 means security practices are described with no formal certification, and 1-2 means there's no documented security or compliance posture. For Scalability, 9-10 means documented rate limits/SLAs comfortably exceed 10,000 MAU load with published autoscaling, 6-8 means the documented limits meet that load with some headroom, 3-5 means the limits are close to or ambiguous relative to that load, and 1-2 means the limits fall short or aren't documented. For Community Support, 9-10 means there's a large, visibly active community across official channels, 6-8 means there's a moderate but real community, 3-5 means the community is small or mostly inactive, and 1-2 means there's little to no public community presence; cite the specific evidence you used (e.g., GitHub star count and recent activity, Stack Overflow tag volume, forum or Discord activity) to land on a band. Calculate Overall Score using Feature Coverage 25%, Compatibility 20%, Integration Complexity 10% (Easy = 10, Medium = 6, Complex = 3), Cost 15% (inverse-scaled so lower cost scores higher), SDK Quality 10%, Documentation Quality 10%, and Security 10%. Break any final ranking ties using the lower estimated monthly cost.

Recommend exactly one primary platform, explaining why it is the best fit, the expected implementation effort in days, the expected long-term maintenance cost, and the main risks to monitor. You may also recommend up to two alternatives, each with a one-line note describing when it would be the better choice.

Create an implementation-ready architecture blueprint for the recommended platform using only publicly documented information. Include a High-Level Integration Architecture, Data Flow, Authentication Flow, Error Handling Strategy, Retry Mechanism, Rate-Limit Handling, Caching Recommendations, Security Considerations, Monitoring & Logging, and Deployment Considerations, with 3-6 bullet points for each section.

Create a new Google Doc or modify "[TCW] - 3rd Party Research - <TICKET-KEY> - <YYYY-MM-DD>" in the "[RelayForge Research Documentation]" folder. Prior to creating the Google doc, ensure that a Google doc with the same name is not present in the folder. If it does, overwrite its contents instead of creating a duplicate.

The document produced is a Google document, not a Markdown file. Make use of Google Docs formatting features. Use Google Docs headings, tables, number and bullet points lists, links, page breaks, and checklist features wherever possible. Use the real Google Docs formatting, not the fake one.

Avoid documenting internal workflow steps inside the deliverable.

It should contain the following sections in the given order: Project Overview, Feature Requirements, Technology Stack, Evaluation Methodology, Comparison Matrix, Recommended Platform, Individual Platform Research (each platform to be researched individually in a different section from the total of five platforms evaluated), Architecture Blueprint, Risks & Mitigation, Cost Analysis, Implementation Checklist, and Final Recommendation. Note the GitHub commit SHA used and the vendor-research as-of date in the Evaluation Methodology section.

Do a check on the selected Jira issue and only that. Begin the research summary comment with the exact line "Automated research completed" so it can be found on reruns; if a comment on the issue already starts with that exact line, edit that comment in place instead of posting a new one. This is separate from the "Research has been started automatically" comment added earlier, which is not affected by this dedup check. The comment should contain a research summary (not exceeding 150 words), the platform that is suggested, the comparison table in Jira table format, the estimated time of integration (in days), the estimated cost (in USD) per month, no more than three risks, and a link to the Google Docs file. Once done, change the status of the issue to In Review.

Send a message directly into the Microsoft Teams channel "[testing client workflows + Engineering]" confirmed earlier. Check the latest 20 messages before sending. Write everything the reader sees like a senior engineer wrote it by hand for teammates, using the target platform's own formatting (Google Docs or Teams) instead of Markdown or plain text. In case one of the messages already has both the ticket key and "Third-Party Research Completed" phrase, do not send a message again and log the reason for it in the last run summary.

Feature: <ticket title>
Suggested platform: <main recommendation>
Alternatives Considered <up to 2 platform names>
Estimated Time to Integrate: <X-Y days>
Estimated Cost Per Month: <amount>

Key Risks (no more than three):

Deliverables:

Third-Party Research Report
Table Comparison
Integration Blueprint Checklist for Implementation
Google Doc: <link>

Never put customer PII, credentials, secrets, or confidential financial data in the Google Doc, Jira comment, or Teams message. If present in the ticket, repository or attachments, substitute with [REDACTED] and log the redaction in the final run summary.

Before posting to Teams, verify that the comparison table contains exactly five platforms, exactly one primary recommendation has been selected, every platform section is complete or explicitly marked "Not available", and the document contains no placeholder text such as TBD, Lorem ipsum, or [fill in]. If any check fails, correct the document and repeat the validation before continuing.

The workflow is complete only when exactly one Jira issue has been moved to In Review with one research summary comment, exactly one Google Doc exists in the [RelayForge Research Documentation] folder with all required sections populated, exactly one Teams message has been posted (or intentionally skipped under the duplicate rule), and a final run report has been returned containing links to the Jira issue, Google Doc, Teams message if posted, the GitHub commit SHA used, the vendor-research as-of date, and the total wall-clock execution time.