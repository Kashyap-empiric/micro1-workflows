Take a look at our web app and put together a plan for taking it mobile, one that's actually ready to build from. The code's in the GitHub repo "kashyapempiricinfotech-sys/webapp-main", main branch. Read it as it stood on 10 July 2026, 18:00 IST, so whichever commit landed latest at or before then. Don't run the app, don't hit any live endpoints. Just reading the code is enough.

First check the work is doable. You'll need read access to the repo, permission to create issues and add attachments in the Jira project "Mobile Build - Test Fixture" (key MBTF) at https://workflow-testing-xgfb79cw.atlassian.net, edit access to create a new Google Doc in the Drive folder "Mobile Build - Test Fixture", and posting rights in the Microsoft Teams "Engineering" channel under the "Testing Client Workflows" team. Jira attachments and the Google Doc's tabs are the two spots most likely to not be fully reachable through your connectors, so if either one balks, fall back to Chrome before deciding it can't be done. If any of that doesn't work, stop and report what failed rather than pushing ahead on a half-broken setup.

Read through the source (routes and endpoints mostly sit in /api and /server/routes) before you write anything down.

Put everything you find into one Google Doc called "Mobile Build Blueprint - webapp-main", using the doc's native tabs so each part of the analysis gets its own tab instead of one long scroll. Start with an Overview tab holding the exact commit SHA you resolved and a one-line summary of what's on each other tab. Then build out an Architecture tab (tech stack, business logic modules, API endpoints, auth flows, third-party integrations), a Screen Inventory tab (every screen and reusable component, what it's for, how often it's reused), a Mobile Stack tab (cross-platform framework, native bridges, testing tools, and CI/CD pipeline, each with at least two real options weighed against each other and a rough monthly cost pulled from the vendor's current published pricing, naming the plan tier you priced), and a User Flow tab (a screen-to-screen flow diagram of how someone actually moves through the app, or a clear step-by-step flow if a real diagram won't render cleanly in the doc). Add whatever other tabs actually earn their place, a data model, a risks/open-questions list, that kind of thing, your call on what the codebase warrants. Only write down what traces to a real file, mark anything inferred as "(inferred)", and use the doc's actual formatting (headings, tables, whatever fits) instead of pasting everything in as flat text.

Then open the Jira work, but keep it small: no more than four issues total in "Mobile Build - Test Fixture" (key MBTF), not one per screen or feature. Group the work into a handful of real phases, something like foundation and auth, the core screens, notifications and analytics, testing and release, and make one issue per phase (fewer than four is fine if the work naturally groups that way), laying them out across Sprint 1 through Sprint 6 with the foundational work in the earliest sprints and anything that depends on it coming after. Each issue needs a full description covering what's in scope for that phase, which sprint numbers it spans, and what "done" looks like for it, plus the relevant tabs of the doc attached to the issue as files, not just linked.

Keep it re-runnable and don't create duplicates: match issues by their exact summary, update in place if there's one match, create one if there's none, and stop and report if there's more than one match.

Last step, post in Microsoft Teams, in the "Testing Client Workflows" team, "Engineering" channel. Actually send it, don't just draft it. Say the blueprint's ready for review, link the doc, and list the Jira issue keys you created or updated.

This is a draft, so don't ship anything: leave every issue in Jira's "To Do" status and don't move anything further, and don't notify anyone beyond that one Teams post. And if any keys, tokens, or credentials show up in the code, never copy them into an issue or the doc, just point at the file and line and flag it under a "Secrets found (review)" tab in the doc.