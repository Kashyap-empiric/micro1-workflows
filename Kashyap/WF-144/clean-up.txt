Return the dedicated WF-144 test fixture to its known clean state after one workflow run. Use only
the exact names below. Do not delete the repository, Jira project, Drive folder, Teams team, or
Teams channel.

Cleanup is limited to workflow changes. Resources the workflow only read or used unchanged are left
alone.

## Clean these exact test destinations

- Jira project: `Mobile Build - Test Fixture` (`MBTF`) at
  `https://workflow-testing-xgfb79cw.atlassian.net`.
- Drive folder: `Mobile Build - Test Fixture`.
- Teams team/channel: `Testing Client Workflows` / `Engineering`.
- GitHub repository: `kashyapempiricinfotech-sys/webapp-main`, branch `main`.

## Reset steps

1. Preserve the completed workflow evidence outside the test accounts before cleaning it.
2. In the Drive folder, delete the workflow-created `Mobile Build Blueprint - webapp-main` Doc and
   any other file created by the workflow. Leave the folder itself in place.
3. The seeded Jira state has exactly one issue. In project `MBTF`, delete every issue except one with
   exact summary `Foundation and auth`, including its attachments. If duplicate seed-summary issues
   exist, delete the duplicates and retain one. Restore the retained seed issue to:
   - status `To Do`;
   - no attachments;
   - a description beginning `TEST SEED: the workflow should update this issue instead of creating another with the same summary.`
4. In the `Engineering` channel, delete the workflow-created review post. The channel is empty in the
   seeded state, so remove any message created by the workflow and leave the channel itself in place.
5. Do nothing to the repository if it is unchanged. If the workflow unexpectedly changed it, restore
   the three-commit fixture history described in data seeding; do not delete the repository.

## Verification

Report success only when:

- the Drive folder is empty;
- `MBTF` contains only the reset `Foundation and auth` seed issue;
- the `Engineering` channel contains no workflow message;
- the repository still exists and has the fixture history; and
- the Jira project, Drive folder, Teams team, and channel still exist for the next seed.

If an exact destination cannot be found or reset safely, stop and report it. Do not widen the scope
to other projects, folders, repositories, teams, or channels.
