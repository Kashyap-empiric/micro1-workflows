Execute this setup as a standalone task in this chat. The instructions below are complete: do not ask for or
require clean-up.txt, a prior chat, run ID, manifest, prior setup result, or mode/command flag. This chat is for
setup only; do not run prompt-def.txt. Reset all matching prior-run output regardless of age. If prior output
remains, perform the named-resource reset below directly, then continue with setup.

This is a stable fixture in dedicated test accounts. Do not create a run ID, run manifest, control sheet, setup inventory, production resource, or a second exact-name resource. Exact names define the test boundary.

## Named resources

Create a missing item; otherwise retain and reuse the exact item below. Stop and report an ambiguity if an exact-name project, folder, repository, sheet, team, or channel is duplicated.

- GitHub repository: sauravempiricinfotech-sys/cahuu-llm-gateway, branch main
- Supabase project: cahuu-observability; tables: request_logs and llm_call_logs
- Drive folder: LLM Cost Attribution Reports - Cahuu
- Google Sheet: LLM Cost & Latency Attribution - Cahuu
- Jira project: Cahuu LLM Gateway Ops, key CLGO
- Teams: Testing Client Workflows → LLM Cost Attribution

The GitHub fixture and two Supabase tables are static model inputs. The Drive folder and Sheet, Jira project, and Teams channel are reusable destinations. Keep the repository, tables, folder, project, and team/channel between runs.

## Direct pre-run reset

Before evaluating the ready state, reset only output a prior model could have changed:

- Restore LLM Cost & Latency Attribution - Cahuu to the four blank baseline tabs and headers specified below;
  remove workflow-created rows, formulas, charts, comments, links, formatting, and added tabs, but retain the
  same Sheet and Drive folder.
- Ensure the two labelled CLGO baseline issues specified below exist, creating either if missing; restore them
  to their baseline fields, then delete every other issue and attachment in that exact test project. Retain
  the project itself.
- Delete every post and reply in the exact LLM Cost Attribution Teams channel. Retain the team and channel.
- Do not alter the GitHub repository or Supabase tables during a normal reset. Only repair one of those named
  sources if it was incorrectly changed by a prior model.

## Stable source fixture

The GitHub main history must contain the canonical private evaluator fixture. The latest commit at or before 2026-07-16 17:05 Asia/Kolkata is the model-facing cutoff; later main commits must be present but out of scope. The cutoff snapshot must require genuine tracing across route modules, router includes, service helpers, and separately loaded prompts. It must contain:

- billable OpenAI Chat Completions or Responses, Anthropic Messages, and Gemini generateContent calls;
- embeddings and moderation calls to list separately;
- commented-out code and a feature-flag-off route with generative calls to exclude;
- an endpoint with no valid traffic;
- a router whose URL prefix conflicts with its include_router tag;
- retries around one logical call;
- one independent two-call route and one output-dependent two-call route;
- a long repeated Anthropic prompt without explicit caching, and a long OpenAI or Gemini prompt where automatic caching makes it non-actionable;
- a harmless test-only secret-shaped value whose location, but never value, can be reported.

The named Supabase tables must retain the canonical fictional data for 2026-07-09 00:00:00 through 2026-07-15 23:59:59 UTC. Use a row primary key distinct from request_id so the dataset can include a duplicate request_id. Include clean 2xx and non-2xx records plus, for distinct endpoints, every screening trap in the signed prompt: null, zero, and negative request latency; a duplicate request_id; an llm provider/model inconsistent with code; and a repeated call_sequence_number within one request. Preserve response lengths, call order, and timestamps necessary to test zero traffic, p50/p95, 30/7 monthly projection, cost attribution, and parallelization reasoning.

Do not reset the repository or tables on a normal setup. Verify their canonical state and repair only that named source if a prior model incorrectly changed it. Never alter another repository, branch, or Supabase table.

## Resettable destination baseline

Before the model begins, verify:

- the target Sheet has exactly these four tabs, in this order, with the listed header row and no workflow data rows, formulas, charts, comments, or added tabs:
  - Cost By Endpoint: Endpoint | Feature | Model(s) | 7d 2xx Requests | Monthly Volume | Prompt Token Method | Est. Tokens / Request | Est. Monthly Cost (USD) | P50 Latency (ms) | P95 Latency (ms) | Opportunity Flags | Ticket / Notes
  - Cost By Feature: Feature | Endpoints | Monthly Volume | Est. Monthly Cost (USD) | Opportunities
  - Pricing Sources: Provider | Model | Input Price | Output Price | Official Source | Checked Date | Notes
  - Run Log: Run Date | Analysis Window | Commit SHA | Status / Exclusions
- CLGO contains exactly two baseline issues:
  - open, To Do, unassigned: [LLM Cost] /api/support/triage, labelled wf188-baseline-open; description: WF-188 test baseline: existing open ticket to update in place.
  - closed, Done, unassigned: [LLM Cost] /api/docs/summarize, labelled wf188-baseline-closed; description: WF-188 test baseline: closed history; it must not block a new ticket.
  Both baseline issues have no comments or attachments.
- LLM Cost Attribution contains no posts or replies.

The model-run account needs read access to the repository and tables, edit access to the target Sheet and the CLGO issues, and permission to post to the exact Teams channel. Do not create a report row, Jira ticket, or Teams post during setup.

## Difficulty and isolation

This must remain a performance test rather than a happy path. Keep the code-to-telemetry mismatches, bad rows, source-priority conflicts, cutoff commits, zero-traffic route, false parallelization signal, provider-specific cache distinction, and open-versus-closed ticket history intact. Do not add model-visible expected results, helper formulas, seeded report rows, ticket templates, or channel posts that reveal an answer.

When finished, summarize the setup in plain language and confirm the named resources and ready-state checks. Report any blocker directly.
