# Round 1 data-seeding controls - LLM cost and latency attribution

Create a fresh immutable code snapshot and cloned observability, Sheets, Jira, and Teams state for every
run `R1-A` through `R1-D`. Tag output `[COST-R1-<run>]`; keep screened-row counts, token math, pricing
workbook, ticket decisions, and reset selectors in a private evaluator manifest.

Seed clean baseline data plus null, zero, and negative latency, duplicate request IDs, code-inconsistent
provider/model rows, and repeated call sequence rows. Include non-generative embeddings/moderation, dead
or commented-out code, zero traffic, dependent calls that are not parallelizable, independent calls that
are, a long uncached Anthropic prompt, and a long automatic-caching provider prompt. Use fixed window
data so p50, p95, monthly projections, and ticket gates have auditable answers. Seed open and closed
ticket histories for update-versus-new behavior.

Create a test-only environment for the Cahuu LLM cost and latency attribution workflow. Do not use production systems, production data, real provider API keys, or real billing credentials. Reuse exact-name resources if they already exist.
Required resources:
GitHub repo: sauravempiricinfotech-sys/cahuu-llm-gateway
Supabase project: cahuu-observability
Google Drive folder: LLM Cost Attribution Reports - Cahuu
Google Sheet inside that folder: LLM Cost & Latency Attribution - Cahuu
Jira project: Cahuu LLM Gateway Ops, key CLGO
Teams team/channel: Testing Client Workflows / LLM Cost Attribution
Important: the repo must contain a commit on main with commit timestamp at or before 2026-07-16 09:00 IST. If you cannot create or locate such a commit, stop and report that the workflow cutoff cannot be tested.
Create the FastAPI repo with:
app/main.py registering routers with app.include_router(..., prefix=..., tags=[...])
app/prompts/shared.py with BASE_SYSTEM_PROMPT, at least 1,800 characters
app/services/llm_clients.py with fake provider wrappers and one clearly fake credential string labeled test-only
Routes covering:/api/chat/respond: one OpenAI chat call, retry wrapper, plus one commented-out disabled call
/api/chat/summarize-thread: two dependent OpenAI calls
/api/docs/summarize: two independent Anthropic calls using long shared prompt, no cache control, no parallelization
/api/docs/translate: one Gemini call inside asyncio.gather() with a non-LLM coroutine
/api/support/triage: three dependent Anthropic calls; first uses long shared prompt without cache control
/api/search/semantic: embeddings only
/api/moderation/check: moderation only
/api/onboarding/welcome-email: code exists but zero traffic

Seed Supabase with exact tables:
request_logs(request_id uuid pk, endpoint_path text, status_code int, total_latency_ms int, created_at timestamptz)
llm_call_logs(log_id uuid pk, request_id uuid fk, call_sequence_number int, provider text, model text, latency_ms int, response_char_length int, created_at timestamptz)
Seed deterministic rows for 2026-07-09 00:00:00 UTC through 2026-07-15 23:59:59 UTC:
/api/chat/respond: 70 successful rows plus 4 429 rows; one gpt-4o call each
/api/chat/summarize-thread: 50 rows; two gpt-4o-mini calls each
/api/docs/summarize: 55 rows; two claude-sonnet-4-5 calls each; p95 above 8,000ms
/api/docs/translate: 40 rows; one gemini-2.5-flash call each
/api/support/triage: 65 rows; one claude-sonnet-4-5 plus two claude-haiku-4-5 calls each; p95 above 8,000ms
/api/search/semantic: 90 rows; one text-embedding-3-small call each
/api/moderation/check: 60 rows; one omni-moderation-latest call each
/api/onboarding/welcome-email: zero rows
On top of those clean counts, seed a small set of deliberately bad rows so the pre-computation screen has something to catch. The per-endpoint counts above are the rows that should survive the screen, these are extras that a correct run drops back out:
/api/docs/summarize: 2 extra request_logs rows with total_latency_ms null and one with total_latency_ms negative (-1); their p95 must not move
/api/support/triage: 1 duplicate request_id already present in its request_logs, and 1 llm_call_logs row logged as provider openai / model gpt-4o even though this endpoint only makes Anthropic calls
/api/chat/respond: 1 llm_call_logs row with a call_sequence_number that repeats an existing sequence number inside the same request_id
A correct run flags and sets these aside, keeps the stated 2xx counts intact, and logs the drop count per table in the Run Log. A run that computes straight off the raw rows will report a skewed p95 for summarize or triage and an off monthly projection.
Create the Google Sheet in advance, because the workflow requires edit access during its initial access check. Add empty tabs with headers:
Cost By Endpoint
Cost By Feature
Pricing Sources
Run Log
Create Jira seed tickets:
Open To Do: [LLM Cost] /api/support/triage
Closed Done: [LLM Cost] /api/docs/summarize
Expected workflow behavior:
Update the open support triage ticket
Create a fresh docs summarize ticket because the old one is closed
Do not create tickets for endpoints that are merely expensive/slow without a real caching or parallelization opportunity
Treat embeddings and moderation as non-generative calls, not billable generative attribution
Include onboarding in the Sheet with zero volume/cost/latency
Seed Teams with a few unrelated messages, but none mentioning the July 9-15, 2026 window or linking the target Sheet.
Final setup report must include:
GitHub repo URL and fixed commit SHA
Supabase project name and seeded table confirmation
Drive folder URL
Google Sheet URL
Jira project key and seed ticket keys/statuses
Teams team/channel confirmation
