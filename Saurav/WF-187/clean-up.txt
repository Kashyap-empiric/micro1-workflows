# Round 1 cleanup - Cross-platform prompt drift

Reset only `[DRIFT-R1-<run>]` reports, tickets, comments, and Teams output. Capture all final Sheet/Jira/Teams
evidence and source references in the evaluation record before cleanup.

1. Delete the run-created report Sheet or restore its tabs to the immutable baseline. Keep frozen repository
   and Supabase source fixtures unchanged.
2. Delete new tagged Jira tickets. For seeded open tickets updated in place, restore baseline summary,
   description, status, and assignee; remove only automated tagged comments. Leave closed history intact.
3. Delete run-tagged Teams posts and temporary official-guidance snapshots, preserving evaluator evidence
   outside the model-visible fixture.
4. Verify no tagged Sheet rows/tickets/comments/posts remain, all four commit SHAs and invocation logs match
   baseline, and the seeded Jira/Teams state is restored. Log the reset result.

Stop if any item is not exactly matched by tag and baseline ID. Never edit prompt source code or delete
untagged governance history.
