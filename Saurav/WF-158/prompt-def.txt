Read every .sol file in contracts in the GitHub repo sauravempiricinfotech-sys/preaudit-fixture-20260717-568de6c (https://github.com/sauravempiricinfotech-sys/preaudit-fixture-20260717-568de6c), on the preaudit-fixture branch. Skip test files (any path containing /test/ or /mock/) and files under 10 lines. Cap the scan at 40 contract files per run. If the folder has more than 40, scan the 40 most recently modified files as of the run date (by last commit date) and list which files got skipped and why at the end of the report.

For each file, check for these vulnerability categories:
Reentrancy - an external ETH or token transfer happens before the contract's own state update in the same function.
Missing access control - a state-changing public or external function has no modifier restricting who can call it, or has a modifier that isn't a recognizable standard (OpenZeppelin's Ownable or AccessControl) or a custom modifier whose logic you can't verify just by reading the file. If you can't confirm what a custom modifier actually checks, don't assume it's safe. Flag it as a finding, name the modifier, and mark the finding's confidence as low.
Integer overflow - any file on Solidity below 0.8.0 not importing SafeMath. For files on 0.8.0 and above, overflow protection is automatic except inside unchecked{} blocks. Any unchecked{} block wrapping arithmetic on a value that comes from user input or an external call counts as a full finding. Unchecked blocks only touching internal loop counters or other values nobody outside the contract can influence do not count.
tx.origin used for authentication instead of msg.sender.
Unchecked return values from external calls (call, delegatecall, send, low-level calls whose bool return isn't checked).
Timestamp dependence - block.timestamp or now used in logic that gates fund release or unlocks.
Selfdestruct reachable by any address without an access check.

If one function triggers more than one category, only list it once, under whichever category ranks highest in this order: Reentrancy, Missing Access Control, Unchecked External Call, Integer Overflow, tx.origin Authentication, Timestamp Dependence, Selfdestruct Exposure. Mention the other categories that also apply in that finding's writeup, but only count the financial risk once for that function.

For every unique OpenZeppelin version pinned in the repo's imports, search Chrome via browser control for CVEs or published exploit writeups tied to that version. Cite exactly 3 per version, ranked by CVSS score where one exists, otherwise by publish date, and only include ones published within the last 3 years as of the run date. If there aren't 3, list what you found and say so, don't pad it with unrelated stuff.

For each finding, score it:
Impact (1-5): 5 if the function can directly move ETH or tokens out or change ownership, 4 if it can permanently lock or destroy funds or state, 3 if it can alter fund-release timing without directly moving funds, 2 if it only touches internal accounting, 1 if it's informational with no fund or state risk.
Likelihood (1-5): 5 if the function is public/external with zero access or reentrancy guard, 4 if it has a guard you can't verify (per the modifier rule above), 3 if it has a verified standard guard that still has a known logic gap like checking tx.origin instead of msg.sender, 2 if the function is internal but reachable through another vulnerable public function, 1 if it needs privileged conditions that are unlikely to happen.
Severity = Impact + Likelihood, range 2 to 10. Tiers: 8-10 Critical, 6-7 High, 4-5 Medium, 2-3 Low.

Financial risk per finding starts with finding the contract's deployed address: check the repo's deployments/, broadcast/, or addresses folder, or the deploy script's logged output, for an address matching that contract's file name and the network from its deploy config. If no deployment address exists for a contract, treat its balance as $0 and note in the finding that it has no on-chain deployment record. Otherwise, look up that address's current ETH and token balance on the block explorer for whatever network the repo's deploy config targets, via Chrome browser control. Financial risk per finding = that balance times an exposure percentage by category: Reentrancy 100%, Missing Access Control on a fund-moving function 100%, Selfdestruct Exposure 100%, Unchecked External Call 75%, Integer Overflow 75%, Timestamp Dependence 50%, tx.origin Authentication 50%. Price any tokens in USD using CoinGecko's rate at run time, pulled via Chrome browser control. Show the math in the finding, not just the final number.

Deployment readiness verdict: Not Ready to Deploy if any Critical or High finding exists. Ready with Recommendations if only Medium or Low findings exist. Ready to Deploy if nothing above informational was found.

Create a Google Doc in the Drive folder "PreAudit Fixture Reports - 2026-07-17 - 568de6c" (https://drive.google.com/drive/folders/1yqT4Nm6FMNSeBTR9qw8CWhnQo2OYy_n7) named "preaudit-fixture-20260717-568de6c Pre-Audit Report - 568de6c - [run date]". If a report for commit 568de6c already exists in that folder, overwrite it instead of making a new one. The doc needs to open with the deployment readiness verdict and close with a summary of files scanned versus skipped. Each finding needs at minimum: title, severity score and tier with the impact/likelihood breakdown, the vulnerable code snippet with file name and line numbers, the financial risk estimate with math shown, the recommended fix, and any related CVE or exploit reference. Everything past that minimum, how you organize the doc, extra sections, is your call.

Draft an email in Gmail to saurav.empiricinfotech@expert.micro1.ai, don't send it. Subject: "Pre-Audit Report Ready: preaudit-fixture-20260717-568de6c - 568de6c". Body needs the readiness verdict, the count of Critical and High findings, and a link to the Google Doc.
