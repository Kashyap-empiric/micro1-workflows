# Round 1 data-seeding controls - smart-contract pre-audit

Create separate private repositories and output folders for `R1-A` through `R1-D`. Every report and
email draft carries `[PREAUDIT-R1-<run>]`; no model sees another run's output. Keep file eligibility,
commit dates, expected scan-cap selection, contract/address mapping, balances, category priority, score
math, and cleanup selectors in an evaluator-only manifest.

Make the fixture hard but fair: more than 40 eligible contracts with current and old commit dates,
test/mock and short-file exclusions, safe control contracts, an unverifiable custom modifier, safe
unchecked loop arithmetic, user-controlled unchecked arithmetic, and a multi-category function. Include
one no-deployment contract, one deployed zero-balance contract, and one funded fixture address. Pin an
OpenZeppelin version with three date-valid public exploit references. Use fixture addresses only.

Create a dummy test setup for a smart contract automated pre-audit workflow.

First check which connectors/tools are available for:
- GitHub
- Google Drive / Google Docs
- Gmail
- Chrome browser control

If any are missing, stop and tell me exactly which ones to add. Note: Chrome may be available through a Chrome skill/browser runtime even if there is no tool named chrome.

If all required connectors are available, create this setup:

1. Create a private GitHub repo named:
   preaudit-fixture-[YYYYMMDD]-[random-short-id]

2. In that repo, create a branch:
   preaudit-fixture

3. Add a `contracts` folder containing a synthetic Solidity fixture:
   - 48 total `.sol` files
   - 2 files under `contracts/test/` or `contracts/mock/` so they should be skipped
   - 1 file under 10 lines so it should be skipped
   - More than 40 eligible files so the workflow must cap at 40 and skip older files
   - Include contracts that intentionally trigger:
     - Reentrancy
     - Missing access control
     - Unchecked low-level call return values
     - Solidity <0.8.0 integer overflow risk without SafeMath
     - `unchecked {}` arithmetic using user-controlled values
     - `tx.origin` authentication
     - `block.timestamp` gating fund release
     - Publicly reachable `selfdestruct`
     - A custom modifier whose safety depends on another contract and cannot be verified locally
   - Include at least one safe OpenZeppelin `Ownable` / `ReentrancyGuard` example.
   - Pin OpenZeppelin imports to one explicit version, e.g. `@openzeppelin/contracts@4.9.2`.

4. Add deployment metadata under:
   deployments/mainnet/addresses.json

   It should include:
   - network: Ethereum mainnet
   - block explorer: https://etherscan.io
   - token price source: https://www.coingecko.com
   - public Ethereum addresses for the fixture contracts, clearly marked as balance lookup test targets only.

5. Make two commits:
   - First commit: old filler/archive contracts, dated about 1 month earlier
   - Second commit: active vulnerable fixture contracts, dated today
   This is so “40 most recently modified files by last commit date” can be tested.

6. Push the repo to GitHub.

7. Create a Google Drive folder named:
   PreAudit Fixture Reports - [YYYY-MM-DD] - [short commit hash]

8. Create a blank placeholder Google Doc inside that folder named:
   [Repo Name] Pre-Audit Report - [short commit hash] - [YYYY-MM-DD]

   This lets the audit workflow test its “overwrite existing report for the same commit hash” behavior.

9. Get my Gmail profile email and use that as the dummy dev lead email. Do not send any email.

When done, return these exact details so I can paste them into the audit prompt:
- GitHub repo URL
- GitHub repo full name
- Branch name
- Contracts folder path
- Short commit hash
- Drive folder name
- Drive folder URL
- Placeholder Google Doc name
- Placeholder Google Doc URL
- Dev lead email
- Expected scan counts: total `.sol`, skipped test/mock, skipped under 10 lines, eligible count, scanned count, skipped by cap count
- Expected vulnerability categories present
