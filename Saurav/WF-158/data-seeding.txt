Execute this setup as a standalone task in this chat. The instructions below are complete: do not ask for or require clean-up.txt, a prior chat, run ID, manifest, prior setup result, or mode/command flag. This chat is for setup only; do not run prompt-def.txt. Reset all matching prior-run output regardless of age.

Use only:
- GitHub: sauravempiricinfotech-sys/preaudit-fixture-20260717-568de6c, branch preaudit-fixture
- Contracts path: contracts
- Drive folder: PreAudit Fixture Reports - 2026-07-17 - 568de6c
- Gmail test mailbox/recipient: saurav.empiricinfotech@expert.micro1.ai

Keep the private GitHub branch and public fixture-address metadata as static model inputs. They must retain the hard fixture: 48 Solidity files, two test/mock exclusions, one file under ten lines, more than 40 eligible contracts with cap-forcing commit dates, safe/unsafe controls for each category, an unverifiable modifier, safe/unsafe unchecked arithmetic, a multi-category function, and an explicit OpenZeppelin version. Never deploy, sign, transfer, alter chain state, or expose the evaluator oracle.

Direct pre-run reset:
- In the exact Drive folder, retain one blank placeholder Doc named preaudit-fixture-20260717-568de6c Pre-Audit Report - 568de6c - <YYYY-MM-DD>; restore it blank and delete all other workflow report files.
- Delete every Gmail draft in the dedicated test mailbox. Never send a draft or delete the mailbox.
- Verify the static repository branch, fixture deployment metadata, and public balances read-only; restore only the named source if a prior model changed it.

Do not scan contracts, research CVEs, create report content, or draft email during setup. When finished, summarize the setup in plain language and confirm the blank placeholder, zero drafts, and static source checks. Report any blocker directly.
